# 2.5 MHz 一倍速多载波并行前端修正与验收记录

日期：2026-07-15
适用分支：当前并行链路开发分支

## 1. 修正目标

本轮不改变 DMR、P25、dPMR、NXDN、TETRA 的协议判据，也不根据某个测试文件的
绝对时间调整 Probe 窗口。目标是修复以下结构性问题：

1. 固定 `-60 dB` 初始噪声底会把高于该值的纯噪声误判为文件起点活动；
2. 错误 Epoch 会提前消耗 DMR 的 1.5 秒 Probe 预算；
3. 锁定制式后的滑窗解码仍同步阻塞采集/UI；
4. 原入口一次只能维护一个选频链，无法同步处理三个已知载频；
5. 2.5 MS/s 与默认 120 kS/s 不构成整数抽取；
6. TETRA 训练搜索存在按比特位置逐点执行的 MATLAB 循环；
7. 0.25x 拉取式演示不能证明真实 1x 连续输入能力。

DMR 最大 Probe 窗口仍为 **1.5 秒**，TETRA 最大 Probe 窗口仍为 **6 秒**。

## 2. 活动检测修正

### 2.1 自适应噪声底

`radio.stream.defaultConfig().activity.initialNoiseFloorDb` 默认改为 `NaN`，表示由首个
可用 IQ 微批自动建立噪声底。每个微批最多取四个 2048 点 FFT 段；对复高斯噪声，
周期图中位数除以 `log(2)` 可估计平均噪声功率。窄带 PMR 信号只占所选基带的一部分，
因此即使信号从首样点开始，也不要求先等待一段静默校准。

若上游候选跟踪器或已标定接收机明确提供有限的 `initialNoiseFloorDb`，检测器继续把它
当作外部先验，兼容原有 PFB 路径和确定性测试。

活动结果新增：

- `snrDb`；
- `instantaneousNoiseFloorDb`；
- `smoothedNoiseFloorDb`；
- `spectralNoiseValid`；
- `noiseFloorSource`。

### 2.2 Epoch 与 Probe 时间基准

Probe 快照继续严格从 `epoch.candidateStartSample` 开始。前导静默只由活动检测器消费，
不计入某一制式的最大 Probe 窗口。回归覆盖：

- 连续 6 秒、约 `-47 dB` 的噪声：0 个 Epoch；
- 同一 4FSK 信号从 0、1.5、6.0 秒出现：候选起点分别为 0、1.5、6.0 秒；
- 原始三载波文件的实际结果：
  - `-300 kHz` DMR：1.500 秒建 Epoch，1.800 秒确认；
  - `+150 kHz` DMR：6.900 秒建 Epoch，7.200 秒确认；
  - `+600 kHz` P25：0.900 秒建 Epoch，1.000 秒进入确认窗口。

不存在“文件必须一开始就有信号”的假设，也没有写入 1.59 秒或 6.9 秒等样例专用参数。

## 3. 多载波与异步连续解码

新增：

```text
+radio/+tuned/multiStreamScannerInit.m
+radio/+tuned/multiStreamScannerFeed.m
+radio/+tuned/multiStreamScannerFinalize.m
```

一个宽带 `IqChunk` 同时扇出到 N 个已知大致载频：

```text
wideband IqChunk
  ├─ DDC(channel 1) -> Activity/Epoch -> five-protocol race -> winner decoder
  ├─ DDC(channel 2) -> Activity/Epoch -> five-protocol race -> winner decoder
  └─ DDC(channel N) -> Activity/Epoch -> five-protocol race -> winner decoder
```

每个通道独立保存：

- DDC/NCO/滤波状态；
- ActivityDetector 与 Epoch；
- Probe 状态、赢家与 generation；
- 连续解码状态、PDU 和积压样点；
- `channel_id`、目标频率和相对频偏元数据。

新增 `lockedDecoderStart/Poll/Cancel`，使锁定后的完整解码也使用进程池。采集调用栈只
提交或轮询任务，不等待 P25/TETRA 完成。若 worker 完成时实时边界已经前移，系统不再
反复执行“必须追到当前最新样点才 LOCKED”的追赶循环；而是立即锁定，并按样点顺序
处理有界积压。`backlogSamples` 与 `overrunSamples` 用来区分正常积压和环形缓冲覆盖。

## 4. DDC 采样率选择

新增 `radio.tuned.resolveInputConfig`：

- 61.44 MS/s 输入保持 120 kS/s，整数抽取 512；
- 2.5 MS/s 输入自动选择 125 kS/s，整数抽取 20；
- 输出奈奎斯特频率必须高于当前 55 kHz DDC 阻带边界；
- 找不到合法整数抽取时明确报错，不使用有理重采样偷偷替代实时 DDC。

## 5. TETRA 性能修正

协议窗口和确认判据未变化，优化仅消除重复计算：

1. 训练序列滑动汉明距离由逐位置 `nnz(xor(...))` 改为等价卷积公式；
2. π/4-DQPSK 四个等间距相位中心的最近误差用模 `pi/2` 折叠计算；
3. 长窗口相位偏移搜索用均匀分布的最多 8192 个 transition 选择网格点，选定后仍在
   全部 transition 上计算最终误差；
4. `swap_bits` 复用 `standard` 相位偏移，`conjugate_swap` 复用 `conjugate` 相位偏移。

原 6 秒 DMR 错配输入的 TETRA 路径约 4.42 秒，本轮实测为 **1.335 秒**，下降约 70%。
真实 TETRA 回归仍输出 685 条事件，训练序列卷积结果另与朴素 XOR 逐点实现逐项比较。

## 6. 去重语义

`radio_live_frontend` 默认 `Deduplicate=false`，满足调试阶段“每取得一块 PDU 就立即
显示”的要求。这里区分两类去重：

- **必须保留的窗口重叠抑制**：相同样点、相同内容因滑窗重解码再次出现时不重复输出；
- **暂时关闭的语义去重**：不同样点但内容相同的空口 PDU 不再合并。

CALL/SESSION 是派生状态而不一定是一块原始空口 PDU。关闭语义去重后，它们可能随解码
窗口重复生成，因此不同异步调度下总记录数可能略有变化。后续优化应把“原始 PDU”、
“状态变化事件”和“会话摘要”分成三种输出类型，而不是重新开启全局内容去重。

## 7. 精简 1x 入口

推荐入口：

```matlab
cd('/home/lzkj/lzkj_workspace/matlab_docs')
startup
radio_live_frontend
```

操作：

1. `Preview 1x`；
2. 依次点击一个或多个谱峰，右侧列表会保留全部选择；
3. `Run decode`；
4. 界面立即显示 `SIGNAL_ON`、`LOCK` 和每条 PDU；
5. `Clear carriers` 可在解码运行中直接取消解码器，频谱回放继续。

精简入口删除瀑布、Max Hold、PDU 表格和 JSON 保存等非必要实时工作，只绘制 4096 个
显示点。文件输入使用 20 ms 块，DDC 内部仍按 10 ms 连续处理，Activity/竞赛仍按
100 ms 基带微批。固定播放延迟默认 100 ms，输入逻辑时间严格按墙钟 1x 推进；解码慢
时增加显式积压，不能通过减慢文件生产速度掩盖。

程序化三载波示例：

```matlab
app = radio_live_frontend( ...
    'DefaultFile', ...
    '/home/lzkj/lzkj_workspace/python_docs/DMR_demo/data/synthesized_wideband_2.5MHz.rawiq', ...
    'ReplayMode', 'once', ...
    'MaxLoops', 1);
cleanup = onCleanup(@() app.Close());

app.StartPreview('StartTimer', false);
app.Step(8);
app.AddOffsetHz(-300e3, 'Refine', false);
app.AddOffsetHz(+150e3, 'Refine', false);
app.AddOffsetHz(+600e3, 'Refine', false);
app.RunIdentification();
```

原 `radio_frontend.m` 继续保留为单载波、完整诊断界面，默认速度也已恢复为 1x。

## 8. 真实验收数据

输入：`synthesized_wideband_2.5MHz.rawiq`，2.5 MS/s，10 秒，CI16。

| 验收项 | 结果 |
|---|---:|
| 核心三 DDC + 异步解码，1x 喂入耗时 | 10.097 s |
| 核心最大输入滞后 | 129 ms |
| 核心等待任务后的总耗时 | 11.72 s |
| 精简 UI 最大输入滞后 | 110 ms |
| 精简 UI 含最终收尾总墙钟 | 12.30 s |
| 通道 1 | -300 kHz，DMR，1 Epoch |
| 通道 2 | +150 kHz，DMR，1 Epoch |
| 通道 3 | +600 kHz，P25，1 Epoch |
| 最终解码积压 | `[0 0 0]` |

真实 `DMR_signal/1.bvsp` 兼容验收仍为：精调 `+1.235391567 MHz`、锁定 DMR、3 个
PDU、1 个 Epoch。

## 9. 测试

新增或扩展：

- `tests.runStreamingPhase1`：高噪声前导静默与时间平移不变性；
- `tests.runStreamingPhase4`：异步 locked decoder；
- `tests.runStreamingPhase6`：语义去重开关；
- `tests.runTunedTransition`：120/125 kS/s 自动选择；
- `tests.runRealtimeFrontendPhase5`：三载波扇出；
- `tests.runRealtimeFrontendPhase6`：精简 UI 全链路；
- `tests.runTetraOptimization`：卷积汉明距离与朴素实现等价。

完整 `tests.runAll()` 已通过：DMR、P25、dPMR、NXDN、TETRA 及所有流式阶段无回归。

## 10. 当前边界

1. 已证明的是 **2.5 MHz、三个已知大致载频**；未证明 60 MHz MATLAB CPU 实时。
2. 频谱/file/DDC 调度仍在 MATLAB UI timer 所在线程；协议 Probe、catch-up 和 locked
   decode 已在进程 worker。真实 SDR 到位后仍需原生生产者、有界 CI16 队列和硬件时间戳。
3. 五制式任务共享固定进程池。载波数量继续增加时必须加入全局调度优先级，不能无限
   增加 worker。
4. DMR、P25、dPMR、NXDN 和 TETRA DMO 已实现真正增量的
   `streamInit/feed/finalize`；TETRA TMO、NXDN48/Type-D 不在当前支持范围内。
5. `overrunSamples>0` 必须视为实时失败；不能静默丢弃或用慢放掩盖。

## 11. Run/Clear 状态机修正（2026-07-15）

### 11.1 根因

旧 `Run decode` 回调按以下顺序执行：

```text
stop timer -> close file source -> warm DDC/process pool
           -> reopen file source -> reset PSD -> reset replay clock
```

因此用户看到的“卡顿后文件似乎重播”是确定性行为，不是显示假象。
`Clear carriers` 又把“`scanner` 字段非空”等同于“解码器正在运行”，已终止或
已 finalize 的对象也会被拒绝，从而抛出 `Stop the decoder before clearing carriers`。

### 11.2 新生命周期

```text
Preview:  prepare pool + reusable DDC slots -> open source -> PSD 1x
Run:      keep source/timer/PSD -> retarget prepared DDCs -> attach scanner
Clear:    cancel race/catch-up/locked futures -> discard decoder queue
          -> clear selections -> remain in PREVIEW
Stop:     cancel decoder -> close source -> STOPPED
EOF:      drain queued IQ -> graceful finalize -> COMPLETED
```

Run 会记录 `decoderRequestedSample` 和 `decoderReadySample`。默认路径中两者相同，
`sourceNextSample` 在 Run 调用前后也必须完全相同；这是“没有重开、回卷或
跳过文件”的回归不变式。

### 11.3 可复用 DDC 槽

`dsp.DigitalDownConverter.CenterFrequency` 是非 tunable 属性，选频后再构建并首次调用会
引入明显 JIT/滤波器初始化延时。新路径在预览开始前创建默认 5 个零中频
DDC 槽并锁定其滤波器；真实载波偏移由槽前的连续相位复本振完成。因此：

- Run 不修改 DDC 的非 tunable 属性；
- `ddcRetarget` 只重置相位、时间戳、缓冲和计数器；
- 原有 internal-mixer DDC 路径保留，不改变离线提取和其他入口的默认行为；
- external/internal 路径的基带输出实测归一化相关系数为 `1.000000000`。

2.5 MHz 下预备 5 个槽约需 10.3 s，这一次性工作在 PSD 开始前以
`PREPARING` 明示；三载波 Run 挂接基准为 0.132 s，隐藏 UI 实测为 0.167 s。
连续三载波 DDC 仍约为 7.6 ms/20 ms 输入块，没有因外部混频破坏 1x 余量。
真实 `synthesized_wideband_2.5MHz.rawiq` 五 worker 验收的 Run 挂接为
0.221 s，且仍正确得到 `DMR / DMR / P25` 三通道结果。

### 11.4 后备队列与清除语义

当用户显式关闭预热，或无法取得预备 DDC 槽时，仍保留异步构建后备路径。
Run 之后到挂接之前的 IQ 以 complex-single 存放在有界队列中，默认上限 15 s；
超限时明确取消解码并继续 PSD，不静默丢样。2.5 MHz 下该硬上限约对应
300 MB IQ 负载，它不是 60 MHz 生产队列方案。

Clear 不执行 tail flush，而是 best-effort 取消每个通道的 race、winner catch-up 和
locked decode future，释放 DDC，然后立即回到 `PREVIEW`。清除前后文件源样点
不变；后续计时器验收中 1.9 s 墙钟推进 1.9 s IQ，输入滞后为 0 ms。

### 11.5 MATLAB 热更新与强制清理

不应在 `radio_live_frontend` 窗口存活时替换函数文件。MATLAB 可能在文件更新后继续
投递旧 timer/异步 Channel 事件，从而使新回调访问旧 `state` 结构。当旧状态没有
`scannerBuildFuture` 等新字段时，旧版 Close 还会先设置 `closing=true`，导致后续关闭
永久早退。

当前实现为新字段提供惰性默认值，Close 不再因 `closing=true` 返回，并额外注册
`DeleteFcn` 作为计时器和解码任务的最终清理路径。如需清除修正前已经卡住的窗口，
在 MATLAB 命令行执行：

```matlab
figs = findall(groot, 'Type', 'figure', ...
    'Name', 'PMR 1x Live Multi-Carrier Frontend');
delete(figs);
timers = timerfindall('Name', 'PMRLeanLiveReplay');
if ~isempty(timers), stop(timers); delete(timers); end
clear radio_live_frontend
```

## 12. 五路近同步实时优化结果（2026-07-15）

在本记录的三路基线之后，新增的五路近同步样例暴露了 worker 饥饿、重复整窗传输、
五路独立 DDC 和 `fixedSpacing` 计时语义等问题。现已完成有界公平竞速、显示/活动检测
限频、调制家族门控、有界历史增量适配、矩阵融合 DDC，以及分类/catch-up 全局优先级。

严格五路运行的最终结果为：五个赢家全部与清单一致，逐路均产生 PDU，6.48 秒输入在
6.844 秒内完成，最大真实输入滞后 0.196 秒，解码队列峰值为 0，验收返回
`functionalPass=true` 和 `continuousDecodeRealtime=true`。完整阶段、命令、性能数据和
适用边界见[2.5 MHz 五路近同步信号合成与验收](2.5MHz五路近同步信号合成与验收.md)。

该结论只覆盖 2.5 MS/s 离线回放和五个已知频点，不扩展为 60 MHz 自动发现或 SDR
硬实时承诺。2026-07-16 后续已完成 DMR、P25、dPMR、NXDN 和 TETRA DMO 的
协议级持续状态化，并将五制式锁定任务迁入持久 Actor。全增量后的三次
压力运行中两次为 0.196～0.205 s，一次为 0.259 s，因此 250 ms 长尾仍不能稳定
保证。最新结果见
[多协议真正增量解码与持久 Worker 第二阶段记录](../recognition/多协议真正增量解码与持久Worker第二阶段记录.md)。

## 13. 输入、频谱与解码消费彻底拆分（2026-07-16）

### 13.1 修正前仍存在的耦合

上一阶段虽然已经把文件生产、PSD 计算和矩阵 DDC 放入不同 actor，但生产者到 DDC
之间的宽带 CI16 块仍先进入 client 的 `PollableDataQueue`，再由 UI timer 回调转发给
DDC 进程。协议 worker 忙时不会停止生产者和 PSD 计算，但 client 一次较长的 scanner
调度仍可能延迟 IQ 转发；因此当时只能称为“计算主体分离”，不能称为完整的生产者/
消费者分离。

MATLAB 的队列边界经过实测后有两个明确限制：

1. `backgroundPool` 生产者不能直接向另一个进程 worker 创建的
   `PollableDataQueue` 发送数据，会得到无效队列错误；
2. `backgroundPool` 任务内看不到 client 的 process pool，把 pool 对象传进去也会得到
   `InvalidSerializedPool`，所以不能简单地把 `RaceCoordinator` 整体搬入后台线程后再
   从那里调用 `parfeval`。

因此本阶段没有继续叠加 DataQueue，而是在生产者与 DDC 之间实现了固定容量、文件映射
的 CI16 共享环形缓冲。

### 13.2 当前执行拓扑

```text
                         best effort，允许显示丢帧
File producer ───────────── DataQueue ──────────> PSD background actor
     │                                                │
     │ 每块只量化一次 CI16                            └─> latest PSD snapshot
     │
     └─> shared IQ ring ──> reserved DDC process ──> 125 kS/s baseband results
          不经过 UI             │                         │
          不允许静默丢块          │                         v
                                matrix DDC          client coordinator
                                                       │
                                                       └─> protocol process workers

UI timer：只轮询进度、最新 PSD、基带结果、状态事件和 PDU，不再搬运宽带 IQ。
```

默认进程配置由原先试验性的 7 个恢复为 6 个：5 个用于协议 Probe/catch-up/locked
decode，1 个长期保留给矩阵 DDC。文件生产者和 PSD actor 使用 `backgroundPool`，不占
这 6 个进程 worker。没有修改任何协议 SPS 或目标采样率。

### 13.3 共享环形缓冲契约

共享缓冲位于 `+radio/+live/sharedIqRing*`，每个槽包含绝对源样点范围、输入块序号、
时间戳、discontinuity、丢样计数、动态量化比例和交错 CI16 IQ。写入顺序为：

```text
invalidate begin/end guard
-> metadata + CI16 payload
-> end guard
-> begin guard
-> publish global write sequence/live sample edge
```

消费者复制前后均检查两个 sequence guard，因而不会接受正在覆盖的半块。环形缓冲默认
覆盖 `ProducerQueueLimitSec=2.0 s`；2.5 MS/s、20 ms 块时为 100 个槽，IQ 主负载约
20 MB。频谱队列达到上限时可以丢旧显示块，并在下一块设置 discontinuity；解码环形
缓冲则绝不静默跳块。若 DDC 的期望序号已经被覆盖，worker 抛出
`RingOverrun`，当前解码明确进入错误，而不是伪装成连续输入。

用户点击 Run 时读取一个稳定 live-edge 快照，DDC 从 `writeSequence+1` 开始，因此不
倒带，也不会把点击前的宽带数据重新送入新 scanner。`Clear carriers` 不再销毁已经
预热的 DDC 进程；它先停止环形消费、清空 DDC 连续状态和旧输出，再复用同一 worker
重新选频。第二次 Run 不承担约 8 秒的 DDC 冷构建时间。

### 13.4 严格五路验收结果

命令保持为：

```matlab
report = tests.runFiveSignal2p5MHzAcceptance( ...
    'Regenerate', false, ...
    'VerifyKnownChannels', false, ...
    'RunFrontend', true, ...
    'StrictFrontend', true, ...
    'Verbose', true);
```

同一 6.48 秒、2.5 MS/s、五路近同步文件连续两次严格运行的结果：

| 指标 | 结果 |
|---|---:|
| 赢家 | `DMR/P25/dPMR/NXDN/TETRA`，全部正确 |
| 每路 PDU | `[8, 3, 10, 30～38, 98～106]` |
| PDU 总数 | 149～165 |
| Run 墙钟 | 6.815～约 6.830 s |
| 端到端实时因子 | 1.052～1.054（包含 EOF flush/finalize） |
| 最大输入/解码积压 | 0.100～0.140 s |
| 生产者 client IQ relay 峰值 | **0 s** |
| DDC 输入积压峰值 | 0.040 s |
| DDC 结果积压峰值 | 0.080～0.140 s |
| DDC 平均/最大计算 | 9.6～10.0/18.9～19.6 ms 每 20 ms 输入块 |
| scanner feed 平均/最大 | 16.7～16.8/173.5～177.1 ms |
| client 协调平均/最大 | 12.7～15.2/258.4～260.8 ms |

`functionalPass=true`、`continuousDecodeRealtime=true`，严格 250 ms 输入积压断言通过。
`tests.runRealtimeFrontendPhase7` 还用一个故意占满唯一协议 worker 2 秒的 Probe 验证：
在没有赢家期间，文件生产样点和 PSD 输入样点仍持续前进；client 宽带 IQ 队列始终为
零；清除载波并第二次 Run 后仍从当前 live edge 继续。

### 13.5 剩余边界

共享环形缓冲解决的是输入生产与 DDC 消费之间的阻塞传播。MATLAB UI 仍必须在 client
上维护 scanner 状态并提交 process-pool Future；受上述嵌套调度限制，不能直接搬入
`backgroundPool`。因此 TETRA 持久 actor 首次提交时仍可能出现约 0.15 秒的单次
scanner 尖峰，UI 绘制会在该回调结束后显示 PSD actor 已计算好的最新快照，但生产、
PSD 计算和 DDC 读取不会停顿。

当前结论只证明 2.5 MS/s 离线文件、五个已知频点和本机 6-process 配置。它不是
60 MHz 或真实 SDR 的硬实时证明。真实设备确定后，生产者改写为 SDR DMA/驱动输入，
仍应保留相同的绝对样点、双 guard、固定容量、显式 overrun 和显示支路可丢/解码支路
不可静默丢的契约；C++ 迁移时再把文件映射原型替换为原生 SPSC 环形队列。

## 14. 五路 1/2/5 分钟持续实时验收（2026-07-16）

### 14.1 方法与判据

新增的 opt-in 入口为 `tests.runLongRealtimeAcceptance`。它把 6.48 秒五路文件以
`continuous-test` 模式连续回放 47 次，总逻辑时长为 304.56 秒；生产速率固定为 1x，
在 60、120 和 300 秒记录逻辑时间、墙钟时间和队列，并在自然 EOF 后等待 DDC、协议
任务和 scanner 完整 finalize。一次 38.744 秒的 process/DDC 冷预热发生在计时前，
不计入 Run 时间。

持续流式实时采用以下判据：

```text
逻辑输入推进速率约等于 1x
队列斜率不为正增长
积压保持在 250 ms 预算内并最终清零
无 shared-ring overrun、无频谱丢块
五路赢家保持正确
```

另行保留更严格的有限文件判据 `Run -> COMPLETED <= 输入时长`。该判据不允许任何固定
播放延迟或 EOF flush，用于和流式实时结论明确区分。

### 14.2 检查点结果

| 目标逻辑时间 | 实际逻辑时间 | Run 墙钟 | 墙钟－逻辑 | 当前解码队列 | 赢家 |
|---:|---:|---:|---:|---:|---|
| 60 s | 60.300 s | 60.563 s | 263 ms | 0 | DMR/P25/dPMR/NXDN/TETRA |
| 120 s | 120.100 s | 120.345 s | 245 ms | 0 | DMR/P25/dPMR/NXDN/TETRA |
| 300 s | 300.160 s | 300.390 s | 230 ms | 0 | DMR/P25/dPMR/NXDN/TETRA |

三个区间的稳态统计为：

| 区间 | 逻辑推进速率 | 队列均值/P95/最大 | 队列斜率 | 墙钟－逻辑斜率 |
|---|---:|---:|---:|---:|
| 0～60 s | 1.000184x | 2.3/0/200 ms | -0.000187 s/s | -0.000194 s/s |
| 60～120 s | 1.000048x | 0.1/0/20 ms | -0.000006 s/s | -0.000054 s/s |
| 120～300 s | 0.999999x | 0.4/0/20 ms | +0.000002 s/s | +0.000001 s/s |

最后 180 秒的 `+0.000002 s/s` 相当于每 1000 秒增加约 2 ms，处于采样和 timer 抖动
量级，不能解释为持续积压。60、120、300 秒三个检查点的当前队列均为零，墙钟与逻辑
时间的差值也从 263 ms 降至 230 ms，没有随运行时长扩大。

### 14.3 EOF 和最终结论

304.56 秒输入在 304.875 秒完成：

```text
RTF                         1.001033
Run 超出输入时长             0.315 s
最大/最终队列                0.200 / 0 s
最大 DDC 输入/结果积压        0.080 / 0.200 s
最大 producer 调度滞后        0.0329 s
频谱丢块                     0
shared-ring overrun          0
```

短文件测试多出的时间约为 0.33～0.35 秒，五分钟测试仍只多出 0.315 秒，证明这部分主要
是固定播放/EOF 收尾延迟，而不是每秒累积的处理欠账。如果吞吐量低于 1x，五分钟的差值
应随 304.56 秒成比例扩大，队列斜率也会持续为正；本次均未发生。

因此，在本机、2.5 MS/s、五个已知频点和当前五制式样例范围内，五路通过 **5 分钟
持续流式软实时** 判据。若采用“有限文件必须在自身时长内完全 finalize”的严格批处理
判据，则仍以 315 ms 之差失败。这个结论不等于操作系统级硬实时，也不覆盖真实 SDR、
60 MHz 输入、不同信噪比或更重的 PDU 输出负载。
